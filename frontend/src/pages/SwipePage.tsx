import { Container } from "@mui/material";

function SwipePage() {
  return (
    <Container
      maxWidth="md"
      sx={{
        display: "flex",
        justifyContent: "center",
        paddingTop: "5rem"
      }}
    >
      Swipe page
    </Container>
  );
}

export default SwipePage;
